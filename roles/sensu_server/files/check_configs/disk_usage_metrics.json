{
    "checks": {
        "disk_usage_metrics": {
            "type": "metric",
            "command": "/usr/bin/ruby /etc/sensu/plugins/disk-usage-metrics.rb",
            "subscribers": [ "all" ],
            "handlers": ["graphite"],
            "interval": 10
        }
    }
}
